import { createTheme } from "@mui/material";

export const themeOptions = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#22C1C1",
    },
    secondary: {
      main: "#FB1B44",
    },
  },
});
